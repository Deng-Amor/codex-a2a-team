{
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "defects"],
  "properties": {
    "status": { "type": "string", "enum": ["PASS", "DEFECTS", "BLOCKED"] },
    "summary": { "type": "string" },
    "defects": { "type": "array", "items": { "type": "object", "required": ["owner", "title", "reproduction"], "properties": { "owner": { "type": "string", "enum": ["frontend-agent", "backend-agent", "test-agent"] }, "title": { "type": "string" }, "reproduction": { "type": "string" } } } }
  }
}
